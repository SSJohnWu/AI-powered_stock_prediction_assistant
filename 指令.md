Python 虛擬環境

# 開啟Powershell權限 
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned

# 建虛擬環境
python -m venv .venv

# 啟動 (PowerShell)
.\.venv\Scripts\Activate.ps1


opencode CLI

# 安裝指令
npm i -g opencode-ai

# 進入 opencode 互動模式
opencode

# 切換模型
/models
切換成deepseek V4


裝套件指令
pip install -r requirements.txt


啟動
streamlit run app.py


gemini api key 申請連結：

aistudio.google.com/app/apikey


github連結：
https://reurl.cc/7E1lRQ
